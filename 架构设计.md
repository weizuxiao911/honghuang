# 架构设计（太初 · 技术视角）

> 读者：工程团队、架构师、想了解太初"怎么搭 / 怎么连 / 怎么跑"的所有人。
> 配套文档：[`功能设计.md`](./功能设计.md)（产品视角，与本文件互引）。

本文档是太初技术架构的事实源。产品形态、功能定义、用户旅程见 [`功能设计.md`](./功能设计.md) —— 同一个系统的两面。

## 1. 分层结构

系统拆成三个相互解耦的运行平面，加上一个独立的分发层：

- **交互平面**（app）：浏览器侧 IDE 容器，承载用户全部可视交互，自身不内置业务逻辑，仅暴露 slot 与 command 总线作为钩子点
- **调度平面**（gateway）：流量网关 + 用户/租户鉴权 + K8s 编排中枢 + SSE 反代，承上启下的中台
- **运行平面**（agent-image）：用户独享 Pod 沙箱，承载 Agent 推理、工具调用、业务执行
- **分发层**（registry）：与运行时解耦的"中央仓库"，只管 VSIX 元数据与包分发，不参与任何运行时决策

这种分层不是工程选择，是产品决策：业务可以仅替换一个平面而保持其余不变。"基座不动"的承诺能落地，靠的是这种横向可替换性。三个可替换的方向：

- 更换底层的 Agent 运行时 → 只换 agent-image
- 更换 IDE 内核 → 只换 app
- 更换调度策略 → 只换 gateway

```mermaid
flowchart TB
    Browser([用户浏览器])

    subgraph 交互平面["用户直接接触"]
      app[app: OpenSumi/CodeBlitz<br/>浏览器侧 IDE 容器<br/>chrome + slot + cmd 总线 + fs 客户端]
    end

    subgraph 调度平面["中台,浏览器不直连"]
      gw[gateway: Spring Cloud Gateway<br/>流量网关 + K8s 编排中枢]
    end

    subgraph 运行平面["用户独享沙箱"]
      rt[agent-image: OpenCode 镜像<br/>Agent 推理 + MCP + A2UI<br/>NAS PVC subPath 持久化]
    end

    subgraph 分发层["与运行时解耦"]
      reg[registry: Spring Boot<br/>VSIX 元数据 + RBAC + CDN]
    end

    Redis[(Redis<br/>taichu:runtime:userId:tenantId<br/>taichu:runtime:by-id:runtimeId)]

    Browser --> app
    app -->|SSE 反代 A2UI<br/>全局命令总线| gw
    app -->|fs 文件通道| rt
    gw -->|创建/回收 Pod<br/>子域名反代| rt
    gw <-->|Runtime 状态| Redis

    app -.->|启动期加载 VSIX| reg
```

## 2. 模块设计

四个模块各司其职，依赖方向统一：

- **registry（上游）**：app 启动期从 registry 拉取 VSIX 元数据清单与扩展包；**registry 必须先于 app 部署**
- **app**：启动后注册 UI slot 与 command 总线，接入 gateway
- **gateway**：接收 app 运行时请求，按需创建 agent-image Pod，通过 SSE 反代把流量路由到对应实例；所有跨层耦合都收敛在 gateway 之内
- **agent-image**：运行平面最小单元，被 gateway 编排；与 app 之间存在 fs 直连通道

### 2.1. 交互平面（app）

**职责**：浏览器侧 IDE 容器、布局骨架（OpenSumi 私有 slot：top/left/main/bottom/right/statusBar/extra 等）、窗口生命周期、VSIX 加载与 activate、全局 command 总线、SSE 通道、fs 文件客户端。同时承担三个**与后端基础设施直连的核心内置服务**，供 VSIX 消费：

1. **登录与会话建立**（GitHub OAuth + `POST /runtime` 拿到 baseUrl）
2. **fs 文件系统通道**（自动连接到沙箱内的文件系统，对 VSIX 暴露文件操作 API）
3. **OpenCode SDK 与事件流**（实例化 OpenCode 客户端并挂到 `window.__TAICHU_OPENCODE__`；订阅 opencode `global/events` 并通过 window event emitter 转发）

VSIX 不直接 `import` opencode SDK、不直接 fetch `${baseUrl}/file...`、不直接接 opencode SSE —— 所有这些操作全部走 app 提供的服务。

**不做**：不内置任何业务 UI、不做 Agent 推理、不直连 K8s、不写业务命令 / 业务状态机。

**与邻接模块关系**：上游 registry（启动期拉 VSIX 元数据并加载）、下游 gateway（控制平面 `POST /runtime` 与数据平面 `{runtimeId}.{suffix}` 子域名）、直连 agent-image（fs 通道走子域名 + OpenCode SDK 通过 gateway 反代到沙箱）。

```mermaid
flowchart TB
    subgraph app["app 模块: 浏览器侧 IDE 容器"]
      direction TB

      subgraph chrome["框架 chrome (OpenSumi 私有布局, 零业务)"]
        layout["layoutConfig<br/>slot: top / left / main / bottom / right / statusBar / extra"]
        cmdBus["全局 command 总线<br/>id: {publisher}.{name}:{action}"]
      end

      subgraph chan["网络通道"]
        boot["runtime 引导<br/>登录后 POST gateway /runtime"]
        reg["registry 客户端<br/>fetchMetadata + 下载 .vsix"]
        fs["fs 文件客户端<br/>/file, /file/content, /pty"]
        sse["SSE 通道<br/>text/event-stream"]
      end

      subgraph core["核心内置服务 (暴露给 VSIX 消费)"]
        opencode["OpenCode SDK 实例<br/>window.__TAICHU_OPENCODE__"]
        eventbus["opencode global/events 转发<br/>window event emitter"]
      end

      subgraph loader["VSIX 加载器"]
        ext["OpenSumi ExtensionService<br/>builtInExtensions + extensionMetadata"]
        act["VSIX activate()<br/>views + commands 注册"]
      end

      subgraph cfg["外置配置"]
        appCfg["appConfig.json"]
        runCfg["runtimeConfig.json"]
        env["ConfigMap: GATEWAY_URL / RUNTIME_HOST_SUFFIX / DEPLOY_ENV"]
      end

      boot --> opencode
      fs --> opencode
      opencode --> eventbus
      ext --> act --> chrome
      cmdBus -.->|插件间唯一合法通讯| act
      cfg -.-> chrome
      cfg -.-> loader
      chrome -->|SSE 反代 A2UI| sse
      chrome -->|文件读写| fs
      chrome -->|读 OpenCode SDK| opencode
      chrome -->|监听 opencode 事件| eventbus
    end
```

### 2.2. 分发层（registry）

**职责**：VSIX 元数据存储、CDN/OSS 包分发、多版本管理与灰度发布、上下架与回滚、RBAC 权限裁剪、审计日志。

**不做**：不参与 Agent 执行、不参与 K8s 调度、不参与运行时决策，是与三个运行平面完全解耦的"中央仓库"。

**与邻接模块关系**：仅下游 app（启动期一次性拉清单 + 按需下包），不与 gateway / agent-image 发生任何调用。

```mermaid
flowchart TB
    subgraph reg["registry 模块: Spring Boot 插件分发"]
      direction TB

      subgraph api["REST API"]
        meta["GET /metadata.json<br/>元数据清单"]
        vsix["GET /vsix/{name}-{version}.vsix<br/>CDN/OSS 代理"]
        mgmt["管理 API<br/>上传 / 灰度 / 回滚"]
      end

      subgraph core["核心服务"]
        parser["VSIX 解析引擎<br/>package.json 校验"]
        rbac["RBAC 权限裁剪<br/>租户 + 用户授权"]
        ver["版本管理<br/>多版本 / 灰度 / 上下架"]
        audit["审计日志<br/>上下架 / 灰度 / 回滚"]
      end

      subgraph store["存储"]
        mysql[("MySQL<br/>extension, extension_version<br/>tenant_extension, user_extension, audit")]
        cdn["CDN / OSS<br/>*.vsix 文件存储"]
      end

      api --> core
      parser --> mysql
      ver --> mysql
      ver --> cdn
      rbac --> meta
      audit --> mysql
    end
```

### 2.3. 调度平面（gateway）

**职责**：同时承担流量网关与 K8s 编排中枢两个角色。流量网关收敛全平台公网流量、统一鉴权、统一 Header 透传、限流熔断、全链路日志、SSE 长连接透传；K8s 编排中枢预置 Deployment/Service/PVC/HPA 资源模板，按需创建用户独享 Pod、用 Redis 双索引维护运行时状态、用 TTL 过期触发自动回收并巡检残留资源。

**不做**：不渲染前端、不解析 A2UI 业务语义、不存储 VSIX 包、不直连业务 Agent 执行逻辑。

**与邻接模块关系**：上游 app（控制平面 `POST /runtime` 与数据平面 `{runtimeId}.{suffix}` 子域名）、下游 agent-image（创建/销毁 Pod、子域名反代转发）。

```mermaid
flowchart TB
    subgraph gw1["gateway 模块: 流量网关"]
      direction TB

      subgraph in["入口"]
        direction LR
        host["Host 路由<br/>主域名 + {runtimeId}.{suffix}"]
        correl["双通道寻址<br/>同源: x-runtime-id<br/>跨端: Host 子域名"]
      end

      subgraph pipe["请求处理管道"]
        direction LR
        auth["鉴权<br/>用户/租户身份校验"]
        hdr["Header 透传<br/>X-User-Id / X-Tenant-Id / X-Deploy-Env / X-Runtime-Id"]
        rate["限流 + 熔断 + 降级"]
        log["全链路日志"]
      end

      sse["SSE 长连接透传<br/>proxy-read-timeout: 3600 / body-size: 100m"]

      in --> pipe --> sse
    end
```

```mermaid
flowchart TB
    subgraph gw2["gateway 模块: K8s 编排中枢"]
      direction TB

      sched["Pod 编排<br/>Deployment 模板创建/销毁"]
      hpa["HPA 水平伸缩<br/>cpu/memory 阈值"]
      pvc["PVC 巡检 + 残留清理"]

      subgraph idx["Redis 运行时索引"]
        r1["taichu:runtime:{userId}:{tenantId}<br/>-> runtimeId"]
        r2["taichu:runtime:by-id:{runtimeId}<br/>-> snapshot JSON"]
        ttl["TTL 过期回调<br/>默认 30 分钟 (可配)"]
      end

      redis[("Redis")]
      idx --> redis
      ttl -.->|过期触发| sched
    end
```

### 2.4. 运行平面（agent-image）

**职责**：单用户独享沙箱，承载全部 Agent 推理、工具调用、业务执行；预装 OpenCode 运行时、内置大模型调用、代码安全沙箱、MCP 工具协议；通过 NAS PVC 双 subPath 隔离工作区与全局配置；输出遵循 A2UI 协议；业务 Agent 插件通过 `opencode.json` 动态加载。

**不做**：不参与调度决策、不参与插件分发、不渲染 UI、不解析 runtime policy。

**与邻接模块关系**：上游 gateway（创建/销毁/子域名反代）、直连 app（fs 通道走 `{runtimeId}.{suffix}`）。

```mermaid
flowchart TB
    subgraph ai["agent-image 模块: 单用户独享 Pod 沙箱"]
      direction TB

      subgraph oc["OpenCode 运行时"]
        core["OpenCode 内核<br/>大模型调用 + 沙箱"]
        mcp["MCP 客户端<br/>反向调用外部业务服务"]
        skills["Skill / Agent / MCP 三层能力拆分"]
        a2ui["A2UI 协议输出<br/>声明式流式组件"]
      end

      subgraph persist["NAS PVC subPath 持久化"]
        ws["workspace/<br/>用户项目工作区"]
        cfg["config/<br/>全局配置"]
      end

      subgraph load["动态加载"]
        ojson["opencode.json<br/>业务 Agent 插件配置"]
        env["Pod ENV<br/>用户/租户身份透传"]
      end

      ojson -.->|动态挂载| core
      env -.->|身份注入| mcp
      skills -.->|业务能力| core
      core --> a2ui
      core --> mcp
      persist -->|fs 文件通道| core
    end
```

## 3. VSIX 业务扩展生命周期

VSIX 业务扩展有自己的生命周期，平台业务能力与容器骨架严格分离：

- **`app/src/`**：维护基于 OpenSumi/CodeBlitz 的框架容器与文件系统
- **`taichu/app/extensions/{name}/`**：维护业务功能交互
- **打包**：按 VSIX 兼容扩展标准开发，编译成 `{name}-{version}.vsix`
- **上架**：传到 `registry/`
- **加载**：`app/src` 运行时通过网络加载

```mermaid
flowchart TD
    subgraph develop["开发侧 - taichu/app/extensions/{name}/"]
      direction TB

      subgraph source["① 源"]
        src["app/extensions/{name}/<br/>src/ + package.json"]
      end

      subgraph build["② 构建"]
        tool["vsce pack"]
        pkg["name-version.vsix"]
      end

      src --> tool --> pkg
    end

    subgraph publish_side["分发侧 - registry"]
      direction TB

      subgraph publish["③ 上架 registry"]
        db[("MySQL<br/>extension, extension_version")]
        cdn["CDN/OSS<br/>*.vsix"]
      end

      pkg --> db
      pkg --> cdn
    end

    subgraph runtime_side["运行时侧 - app"]
      direction TB

      subgraph load["④ 加载 - app 启动期"]
        meta["GET /metadata.json"]
        dl["下载 .vsix"]
        ext["ExtensionService"]
      end

      subgraph run["⑤ 运行"]
        act["activate()<br/>注册 views/commands"]
        chrome["chrome<br/>slot + 总线"]
      end

      db --> meta --> ext
      cdn -.-> dl --> ext
      ext --> act --> chrome
    end

    develop --> publish_side --> runtime_side
```

完整的六步链：

- **1. 源**：业务扩展源落在 `taichu/app/extensions/{name}/`，TS + VSIX 兼容 Manifest；`package.json` 声明 `engines.vscode` 与 `sumiContributes.browserMain/workerMain`，骨架为 `src/extension.ts` + `src/views.tsx`
- **2. 构建**：`scripts/build.js` 把源 + `out/` + 资源打成 `{name}-{version}.vsix`
- **3. 上架**：`.vsix` 推到 `registry/`，解析 `package.json` 入库 + CDN/OSS；`/metadata.json` 实时反映清单
- **4. 加载**：`app/src` 启动时 `registryClient.fetchMetadata()` 拉清单，对每个条目从 CDN 下载后装入 `ExtensionService`（即 `runtimeConfig.extensionMetadata`）
- **5. 运行**：VSIX `activate()` 阶段向 `LayoutService` 与 `CommandService` 注册贡献；框架 chrome 只暴露 slot 与命令总线，具体命令 / 视图由 VSIX 提供
- **6. 更新 / 卸载**：registry 删除或更新元数据后下次 app 重启生效；本期不实现运行时热替换

强制边界：

- `app/src` 不写业务 UI、不写业务命令、不写业务状态机
- 新增业务 = 新建 `taichu/app/extensions/{name}/` + 上架 registry；不动 `app/src` / `gateway` / `agent-image`
- 跨 VSIX 联动只通过全局 command ID；禁止跨包 import
- 内置 VSIX（`chat-window` / `session-manager`）的源码同样在 `taichu/app/extensions/`，按这套生命周期构建上架，运行时由 app 经网络加载，绝不打进 `app/src` 主包

## 4. 业务流程

端到端六步流程（产品视角见 [`功能设计.md §4`](./功能设计.md#4-业务流程端到端)）：

1. 登录 + app 加载
2. runtime 创建或复用
3. fs 通道贯通（贯穿始终）
4. Agent 推理 + MCP 工具调用
5. A2UI 流式回写到前端
6. TTL 自动回收（PVC 数据留存）

**跨层边界**（任何模块都不得越界）：

- **app**：不直连 K8s、不解析 A2UI
- **gateway**：不渲染 UI、不解析 A2UI 业务语义
- **agent-image**：不参与调度决策
- **registry**：不参与运行时

```mermaid
sequenceDiagram
    actor U as 用户
    participant App as app
    participant GW as gateway
    participant R as Redis
    participant K as K8s
    participant AI as agent-image
    participant FS as 文件通道 (fs)
    participant MCP as 外部 MCP 服务

    U->>App: 浏览器打开 app 入口
    App->>GW: 登录 + POST /runtime (X-User-Id, X-Tenant-Id)
    GW->>R: 查 taichu:runtime:userId:tenantId
    alt 无可用实例
      R-->>GW: miss
      GW->>K: 创建 Pod (Deployment 模板)
      K-->>GW: Pod Created
      GW-->>App: SSE: SCHEDULED
      K->>AI: 启动 OpenCode + 加载 Agent 插件
      AI-->>GW: SSE: READY
    else 复用已有实例
      R-->>GW: snapshot
      GW-->>App: SSE: READY
    end
    GW-->>App: 返回 baseUrl = {runtimeId}.runtime.domain
    App->>FS: 建立 fs 通道 (HTTP /file, /file/content, /pty)
    FS->>AI: 透传到 agent-image Pod 进程
    Note over App,AI: 编辑器/MCP/Agent 产生<br/>的所有文件读写经 fs 同步
    U->>App: 编辑文件 / 发起对话
    App->>FS: HTTP PUT /file/content
    FS->>AI: 写入 Pod 内文件
    App->>GW: 全局 command 转发 (对话/任务)
    GW->>AI: A2UI/SSE 透传
    AI->>MCP: 反向调用业务服务
    MCP-->>AI: 业务结果
    AI->>FS: 落盘代码 / 中间产物
    AI-->>GW: A2UI 流式输出
    GW-->>App: SSE 反代
    App-->>U: 前端流式渲染
    Note over AI,R: 闲置超 TTL
    R-->>GW: key 过期
    GW->>K: 销毁 Pod + 巡检 PVC
    Note over App: PVC 数据留存,下次 fs 通道秒级重建
```

## 5. 接口契约

跨模块每一跳走约定接口，禁止跨层直连。

| 边界 | 接口 | 关键约定 |
|------|------|----------|
| registry 暴露 | `GET /metadata.json` | 元数据清单，启动期 app 只读访问 |
| registry 暴露 | `GET /vsix/{name}-{version}.vsix` | CDN/OSS 代理 |
| app ↔ registry | `registryClient.fetchMetadata()` | 启动期一次 |
| app ↔ gateway（控制平面） | `POST /runtime`（登录 + 创建/复用 + 取 baseUrl） | `X-User-Id` / `X-Tenant-Id` 必带 |
| app ↔ gateway（数据平面） | 主域名 + 子域名 `{runtimeId}.{suffix}` | `Host` 路由 |
| 通用 Header | `X-User-Id` / `X-Tenant-Id` / `X-Deploy-Env` / `X-Runtime-Id` | gateway 注入并透传 |
| **app ↔ agent-image（fs 文件通道）** | `GET/POST /file`、`GET /file/content`、`POST /pty` | baseUrl = `{runtimeId}.{suffix}` 由 gateway 登录后下发；贯穿 IDE 全程 |
| SSE 反代 | `text/event-stream` | `proxy-read-timeout: 3600` / `proxy-send-timeout: 3600` / `proxy-body-size: 100m` |
| gateway ↔ agent-image | `POST /runtime` 创建 + SSE 状态流 | `type`: `INITIAL_STATE` / `CREATED` / `SCHEDULED` / `READY` / `FAILED` / `TERMINATING` / `TERMINATED` |
| agent-image 对外 | A2UI（出，前端增量渲染）+ MCP（入，Pod ENV 透传用户身份） | 反向调用外部业务服务 |
| **app → VSIX（OpenCode SDK）** | `window.__TAICHU_OPENCODE__` | app 登录后用 baseUrl 实例化并挂到全局；VSIX 复用实例，**禁止**自己 `import { createOpencodeClient }` |
| **app → VSIX（OpenCode 事件）** | `window` event emitter（`opencode:event` 派发 message.updated / part.updated / session.status / session.idle / session.error 等） | app 订阅 opencode `global/events` SSE 后转发；VSIX 用 `addEventListener` 消费 |
| 跨 VSIX / 跨模块通讯 | 全局 command ID `{publisher}.{name}:{action}` | 唯一合法通道；禁止任何形式的跨包 import |

## 6. 数据模型

四处持久化按所属模块各自归位：

- **MySQL（registry）**：保存 VSIX 元数据
  - `extension`：publisher / name / engine / activationEvents
  - `extension_version`：版本、vsix 存储路径、CDN URL、发布状态、灰度比例
  - `tenant_extension` / `user_extension`：租户 / 用户授权范围
  - `audit`：上下架 / 灰度 / 回滚日志
- **Redis（gateway）**：双索引支撑调度横向扩展
  - `taichu:runtime:{userId}:{tenantId}` → runtimeId（按用户查）
  - `taichu:runtime:by-id:{runtimeId}` → snapshot JSON（按 ID 反查）
  - 两条都带 TTL（默认 30 分钟，可配）；过期自动触发 Pod 销毁 + PVC 巡检
- **NAS PVC（agent-image）**：subPath 双目录隔离
  - `workspace/`：用户项目工作区
  - `config/`：全局配置
  - Pod 销毁后数据留存，重启可恢复
- **配置外置**
  - `app/ConfigMap`：注 `GATEWAY_URL` / `RUNTIME_HOST_SUFFIX` / `DEPLOY_ENV`
  - `agent-image/opencode.json`：动态加载业务 Agent 插件；新业务无需重新构建基础镜像

## 7. 部署架构

- **运行底座**：K8s 生产多节点 / 本地 docker-desktop 单节点
- **命名空间（按职责切三块）**
  - `taichu-infra`：app / gateway / registry / redis
  - `taichu-runtime`：编排支持
  - `taichu-agent-image`：用户独享沙箱，按 label 做硬隔离
- **Ingress**：nginx-ingress-controller，对 app 反代参数 `proxy-read-timeout: 3600` / `proxy-send-timeout: 3600` / `proxy-body-size: 100m`
- **镜像策略**
  - 各模块独立 image
  - 构建期 ARG 注 `EXTENSION_REGISTRY_URL`
  - 运行期 ConfigMap 注 `GATEWAY_URL` / `RUNTIME_HOST_SUFFIX` / `DEPLOY_ENV`
  - agent-image 额外 ENV 透传用户身份
  - 新业务通过动态加载 `opencode.json` 而非重新构建镜像

```mermaid
flowchart TB
    Browser([用户浏览器])

    subgraph K8s["docker-desktop / K8s cluster"]
      direction TB
      Ingress[nginx-ingress-controller<br/>LoadBalancer: localhost:80/443]

      subgraph taichu-infra
        app[app Pod]
        gw1[gateway Pod]
        gw2[gateway Pod]
        redis[(redis)]
        reg[registry Pod]
        cm[ConfigMap]
      end

      subgraph taichu-agent-image
        rt1[Pod user-1<br/>+ PVC-1]
        rt2[Pod user-2<br/>+ PVC-2]
      end
    end

    Browser --> Ingress
    Ingress --> app
    Ingress --> gw1
    gw1 -.list/watch.-> redis
    gw1 --> rt1
    gw1 --> rt2
    app -.启动期拉清单.-> reg
    cm -.env.-> app
    cm -.env.-> gw1
```

- **资源模板（gateway 预置）**
  - Deployment：跑沙箱
  - Service（ClusterIP / NodePort / LoadBalancer）：寻址
  - PVC（NAS）：持久化
  - HPA：自动扩缩
  - ConfigMap / Secret：配置 + 凭据
- **多租户模型**："一户一 Pod"
  - OpenCode 进程独享
  - 流量按子域名匹配 runtimeId 路由
  - TTL 到期自动销毁，PVC 留存数据
- **凭据与身份**
  - 敏感信息不落库（K8s Secret 或 ENV 运行时注入）
  - 用户/租户身份由 gateway 注 Header → agent-image Pod 透传到 MCP 反向调用
  - 全链路无明文凭据落地

## 附录 术语与简称

| 术语 | 说明 |
|------|------|
| Taichu / 太初 | 本项目代号 |
| App | 浏览器侧 IDE 容器（`taichu/app/`） |
| Runtime | 用户独享沙箱，即 gateway 创建并维护的 agent-image Pod |
| Registry | VSIX 资产分发中心（`taichu/registry/`） |
| Gateway | 调度平面，流量网关 + K8s 编排中枢（`taichu/gateway/`） |
| Agent-image | 运行平面最小隔离单元（`taichu/agent-image/`） |
| VSIX | **VS Code 扩展包格式**（vsce / open-vsx 标准）；OpenSumi/CodeBlitz 实现 VS Code 扩展协议，故能直接加载 VSIX |
| OpenCode Plugin | OpenCode 后端插件 |
| A2UI | Agent-to-UI 流式 UI 协议 |
| MCP | Model Context Protocol |
| SSE | Server-Sent Events |
| 工作流总线 | app 全局 command ID，约定 `{publisher}.{name}:{action}` |
| HPA / PVC / TTL / RBAC / CDN | K8s 与运维通用术语 |