# 开源 Agent 产品落地方案
## 总项目名称：洪荒（Honghuang）
根仓库：honghuang
目录结构
```
honghuang
├── zifu（紫府）
├── langhuan（琅嬛）
├── taixu（太虚）
└── dongfu（洞府）
```
整体定位：开箱即用通用Agent产品基座，**胶水哲学为项目核心思想**。平台仅提供标准化连接层与运行规范，不实现任何业务逻辑、不修改底层开源内核；业务交互统一基于VSIX插件、Agent能力统一基于OpenCode Plugin，仅通过新增插件、配置即可快速落地可运营Agent产品。
系统分为交互平面、Agent运行平面两套独立可迭代平面，调度平面作为中间通信桥梁，三层完全解耦、互不侵入内核。

## 一、zifu（紫府）
### 技术栈
OpenSumi Web纯前端内核，无Node.js运行时，浏览器原生Fetch、SSE，完整兼容VSIX扩展规范
### 核心定位
交互平面底层容器宿主，纯前端标准化交互底座，仅负责全局布局、窗口生命周期、插件通信串联，无业务代码、无内置Agent交互逻辑
### 核心功能
1. 提供标准化布局骨架、多面板分区、窗口全生命周期管理，通过builtInExtensions配置统一加载全部VSIX业务插件；
2. 内置全局command命令通信总线，各VSIX插件仅通过标准命令ID完成联动，禁止跨包直接导入，侧边栏、对话、状态栏插件完全隔离；
3. 统一封装SSE长连接通道，仅对话类VSIX处理A2UI流式UI渲染，其余插件通过标准命令转发交互指令；
4. 仅承担容器渲染、插件生命周期管控、跨模块消息中转，全部对话界面、功能操作、用户交互均由外部VSIX插件实现。
### 边界约束
纯浏览器运行，不直连Agent运行时、无后端逻辑、不硬编码服务凭据，所有请求统一经网关转发。

## 二、langhuan（琅嬛）
### 技术栈
SpringBoot、MySQL、Redis、对象存储OSS、CDN、VSIX包解析引擎、RBAC权限校验组件
### 核心定位
VSIX插件资产分发中心，统一托管前端业务扩展包，为紫府提供插件拉取、版本管控标准化服务
### 核心功能
1. 存储各类业务VSIX插件，维护插件元数据、兼容范围、依赖关系，管理多版本迭代、灰度发布、上下架、版本回滚；
2. 插件静态资源同步至CDN，对外提供高速下载接口，支撑紫府前端增量拉取、更新VSIX插件；
3. 基于用户、租户权限区分可用插件范围，完成插件完整性、安全性校验。
### 边界约束
仅负责前端VSIX资源存储与分发，不参与Agent任务执行、K8s资源调度、运行时业务逻辑处理。

## 三、taixu（太虚）
### 技术栈
Spring Cloud Gateway、WebFlux响应式框架、Fabric8 K8s客户端、Redis
### 核心定位
全局调度平面，流量网关+K8s运行时编排中枢，承接交互平面请求，标准化管理用户专属Agent隔离沙箱
### 流量网关模块
1. 收敛全平台公网流量，区分主域名平台接口、子域名运行实例路由，通过Redis维护runtimeId与访问地址映射；
2. 统一完成全局鉴权、用户/租户身份Header透传、接口限流、熔断降级、全链路日志、SSE长连接透传；
3. 双通道寻址规则：同源请求依靠x-runtime-id Header定位实例，跨端跳转依靠子域名Host匹配路由。
### K8s编排调度模块
1. 预置标准化Deployment、Service、PVC、HPA资源模板，接收前端指令后动态创建用户独立Pod；
2. Redis双索引存储运行时状态，支持userId、runtimeId双向检索，自带TTL过期机制；
3. 依靠Redis过期自动触发闲置Pod回收、集群健康巡检、残留PVC清理、资源用量统计；
4. 实现运行时反向代理，将前端A2UI请求转发至对应洞府沙箱。
### 边界约束
仅执行调度决策与流量转发，不渲染前端界面、不解析A2UI业务协议、不承载Agent业务执行逻辑。

## 四、dongfu（洞府）
### 核心资产
标准化Docker镜像、容器挂载契约、OpenCode运行时、后端Plugin加载框架
### 核心定位
Agent运行平面最小隔离单元，单用户独享Pod沙箱，承载全部Agent推理、工具调用、业务执行逻辑
### 核心功能
1. 预装完整OpenCode运行环境，内置大模型调用、代码安全沙箱、MCP工具标准协议；
2. NAS PVC双subPath挂载，隔离用户项目工作区、全局配置，Pod销毁后持久数据保留，重启可快速恢复环境；
3. 通过opencode.json配置动态拉取业务Agent插件，新增业务无需重新构建基础镜像；
4. 通过MCP协议反向调用外部业务服务，用户身份由Pod环境变量注入透传，复用统一鉴权链路；
5. 遵循A2UI协议输出流式UI数据，经太虚网关通过SSE推送至前端对话VSIX渲染展示。
### 后端运行规范
1. A2UI协议：声明式流式UI标准，Agent侧生成组件结构，前端本地管控表单双向绑定；
2. Skill/Agent/MCP三层能力拆分，业务插件通过独立Git地址动态加载；
3. 底层运行镜像与业务能力完全解耦，镜像升级不会覆盖用户持久化配置。

## 完整产品使用闭环
1. 用户部署整套洪荒基座，浏览器访问紫府前端容器，从琅嬛插件市场一键安装业务VSIX插件；
2. 用户在VSIX插件中完成对话、任务配置，交互指令通过紫府全局通信总线转发至太虚调度网关；
3. 太虚校验用户权限，查询Redis判断是否存在可用运行实例，无实例则调用K8s API创建专属洞府沙箱Pod；
4. 洞府挂载用户独立PVC持久数据，加载业务Agent插件，完成大模型调用、MCP业务工具执行；
5. Agent生成A2UI流式数据，经太虚网关SSE长连接回传至紫府对话插件，完成前端可视化渲染；
6. 实例闲置达到TTL阈值后，Redis自动过期触发太虚销毁Pod释放计算资源，用户PVC持久数据留存，再次访问可快速重建运行环境。

## 胶水核心思想说明
本项目以胶水思想为底层设计核心，平台本身不重复造底层基础设施、不固化业务功能，仅作为标准化连接层：
1. 粘合成熟开源底层：统一封装OpenSumi、OpenCode、K8s、LLM网关等开源组件的对接逻辑，不修改底层源码；
2. 粘合前后端业务扩展：定义VSIX前端插件、OpenCode后端插件两套统一标准，所有业务逻辑全部收敛至插件层；
3. 粘合全链路通信与资源：统一规范流量路由、用户隔离、运行时生命周期、跨模块消息交互；
4. 最终价值：底层开源组件、上层业务插件完全解耦，业务迭代仅新增插件配置，无需改动基座内核，实现快速打造可运营Agent产品。