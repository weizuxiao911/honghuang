---
# client 命名空间
apiVersion: v1
kind: Namespace
metadata:
  name: taichu-infra
  labels:
    client: client
    module: taichu
---
# client ConfigMap: 用户持久化 .env 所在 hostpath + gateway 等运行时配置
apiVersion: v1
kind: ConfigMap
metadata:
  name: client-config
  namespace: taichu-infra
  labels:
    client: client
    module: taichu
data:
  GATEWAY_URL: "http://gateway.taichu.localhost"
  RUNTIME_HOST_SUFFIX: "runtime.taichu.localhost"
  DEPLOY_ENV: "dev"
---
# client Deployment
apiVersion: apps/v1
kind: Deployment
metadata:
  name: client
  namespace: taichu-infra
  labels:
    client: client
    app.kubernetes.io/component: platform
    app.kubernetes.io/part-of: taichu
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:

    matchLabels:
      client: client
  template:
    metadata:
      labels:
        client: client
        app.kubernetes.io/component: platform
    spec:
      containers:
        - name: client
          image: taichu/client:dev
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          envFrom:
            - configMapRef:
                name: client-config
          env:
            - name: ENV_FILE
              value: /etc/taichu/.env
          volumeMounts:
            - name: user-env
              mountPath: /etc/taichu
              readOnly: false
          resources:
            requests:
              cpu: "100m"
              memory: 256Mi
            limits:
              cpu: "1000m"
              memory: 1Gi
          livenessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /api/health
              port: http
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
      volumes:
        - name: user-env
          hostPath:
            path: /tmp/taichu-client
            type: DirectoryOrCreate
---
# client Service
apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: taichu-infra
  labels:
    client: client
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:

    client: client
---
# client Ingress: client.taichu.localhost
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: client-ingress
  namespace: taichu-infra
  labels:
    app.kubernetes.io/component: platform
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-body-size: "100m"
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
    - host: client.taichu.localhost
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: client
                port:
                  number: 80